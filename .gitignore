# ── فولدر التجربة مع الباك والفرونت (RestoMind clones) — ما يترفعش ──
test/

# ── بيئة بايثون الافتراضية ──
.venv/
venv/
env/

# ── ملفات بايثون المؤقتة ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── بيانات مولّدة / حالة وقت التشغيل (بتتولّد تلقائيًا) ──
data/*.parquet
data/models/
# حالة الـ registry فيها تاريخ مبيعات كل المطاعم — عمرها ما تترفع.
# بنتجاهل أي JSON جوه data/ (بدل ما نسمّي ملف واحد وننسى اللي بعده)،
# وبعدين نستثني الملفات المصدرية بالاسم.
data/*.json
!data/market_priors.json
# الاسم القديم قبل التحويل من pickle لـ JSON — سايبينه عشان أي نسخة قديمة
# لسه عندها الملف ده.
data/registry_state.pkl

# ── أسرار وإعدادات محلية ──
.env
.env.*
!.env.example

# ── سجلات ومؤقتات ──
*.log
mongo-data/
scratchpad/

# ── نظام التشغيل / المحرر ──
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

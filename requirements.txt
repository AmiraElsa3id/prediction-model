convertdate==2.4.1
fastapi==0.139.2
hijridate==2.3.0
httpx==0.28.1
lightgbm==4.7.0
numpy==2.5.1
pandas==3.0.3
plotly==6.9.0
pyarrow==24.0.0
pydantic==2.13.4
pymongo==4.17.0
pytest==9.1.1
scikit-learn==1.9.0
streamlit==1.59.2
tavily-python==0.7.27
# Required by `zoneinfo` on Windows, which has no system IANA database. Was only
# ever present transitively; the surplus scan now depends on Africa/Cairo
# resolving, so declare it.
tzdata==2026.3
uvicorn==0.51.0
